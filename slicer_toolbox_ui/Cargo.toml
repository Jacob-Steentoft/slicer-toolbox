[package]
name = "slicer_toolbox_ui"
version = "0.1.0"
authors = ["jacob"]
edition = "2024"
build = "build.rs"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
slint = { version = "1.14.1", features = ["default"] }
rfd = { version = "0.15.3" }
slicer-toolbox-core = {path = "../slicer-toolbox-core"}

[build-dependencies]
slint-build = "1.14.0"